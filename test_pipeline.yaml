description: Pipeline for processing csv data
input:
  file_pattern: '*.csv'
name: sample_sales
processing:
  engine: pandas
  operations:
  - params:
      operations:
      - remove_empty_rows
      - trim_strings
    type: clean
profiling:
  enabled: true
  generate_report: true
  output_path: ./reports/sample_sales_profile.html
source:
  config:
    base_path: ./data/raw
    delimiter: ','
    encoding: utf-8
  type: csv
storage:
  destination: sample_sales_data
  mode: append
  type: postgresql
validation:
  auto_generate_expectations: true
  enabled: true
  suite_name: sample_sales_validation
